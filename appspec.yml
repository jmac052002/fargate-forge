version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: <TASK_DEFINITION>
        LoadBalancerInfo:
          ContainerName: "fargate-forge-app"
          ContainerPort: 8000
        PlatformVersion: "LATEST" 